interface User {
    // method 1: registered users can send load
    //           > sender and recipient cant be the same
    //           > balance check
    String getName();
    String getNumber();
    Double getBalance();
}

